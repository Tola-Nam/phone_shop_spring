spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phoneshope
    username: tola_dev
    password: namtola123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
