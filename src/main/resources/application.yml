server:
  servlet:
    context-path: /api

app:
  cors:
    allowed-origins: ${ALLOW_ORIGINS:http://localhost:4200,http://localhost:5173}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phoneshope
    username: tola_dev
    password: namtola123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
